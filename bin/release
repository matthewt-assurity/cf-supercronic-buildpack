#!/usr/bin/env bash
# bin/release <build-dir>
set -e

BUILD_DIR=$(cd "$1/" && pwd)

BUILDPACK_DIR=$(dirname $(readlink -f ${BASH_SOURCE%/*}))

export SUPERCRONIC_ROOT="${SUPERCRONIC_ROOT}"

cat <<EOF
---
default_process_types:
  web: ${SUPERCRONIC_ROOT}"
EOF

echo "Got to the end of release"